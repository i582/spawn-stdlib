module runtime

pub const (
	// OS is the name of the operating system the program is running on.
	// Actual value is set by the compiler.
	OS = ''
	// ARCH is the name of the architecture the program is running on.
	// Actual value is set by the compiler.
	ARCH = ''
)
