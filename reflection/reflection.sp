module reflection

// Struct is a marker interface for all struct types.
pub interface Struct {}

// Interface is a marker interface for all interface types.
pub interface Interface {}

// Union is a marker interface for all union types.
pub interface Union {}

// Enum is a marker interface for all enum types.
pub interface Enum {}

// ArrayTy is a marker interface for all array types.
pub interface ArrayTy {}

// MapTy is a marker interface for all map types.
pub interface MapTy {}

// Ref is a marker interface for all reference types.
pub interface Ref {}

// Pointer is a marker interface for all pointer types.
pub interface Pointer {}

// Instantoation is a marker interface for all instantiation types.
pub interface Instantiation {}

// Tuple is a marker interface for all tuple types.
pub interface Tuple {}
